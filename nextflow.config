params {
    reads  = null
    outdir = null
}

profiles {
    docker {
        docker.enabled = true
    }
}

process {
    withName: 'GENERATE_OVERLAPS' {
        cpus   = 32
        memory = 128.GB
    }
}
